
<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass"></i></a>
      <div class=" d-md-none"><h3 style="color: #fe424d;">Airbnb</h3></div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        <!-- Search bar inside the navbar (for md-large screens only) -->
        <div class="navbar-nav ms-auto search-bar-inside d-none d-md-block">
          <form class="d-flex" role="search" action="/listings/search" method="post">
            <input class="form-control me-2 search-input" type="search" placeholder="Search by Country" name="search">
            <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
        </div>
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Airbnb your home</a>
          <% if(!currUser) { %>
            <a class="nav-link" href="/signup"><b>Sign up</b></a>
            <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>
          <% if(currUser) { %>
            <a class="nav-link" href="/listings/<%= currUser._id %>/bookings">Show All Bookings</a>
            <a class="nav-link" href="/logout"><b>Log out</b></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>


<!-- Sticky Search Bar for small screens -->
<div class="sticky-search-bar d-md-none">
  <form class="d-flex" role="search" action="/listings/search" method="post">
    <input class="form-control me-2 search-input" type="search" placeholder="Search by Country" name="search">
    <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
  </form>
</div>